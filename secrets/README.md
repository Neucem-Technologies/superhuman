# Secrets

Paste testing API keys in **`.env`** in this folder. That file is gitignored.

`.env.example` is the committed template. Vite loads this folder (`envDir: secrets`). Empty values keep the mock feeds. Restart the preview after changing keys.
