# Secrets

Paste connector API keys in **`.env`** (this folder). That file is gitignored.

`secrets/.env.example` is the empty template. Vite loads this folder (`envDir: secrets`). Empty values keep the mock feeds.
